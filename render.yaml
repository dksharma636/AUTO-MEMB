services:
  - type: web
    name: telegram-channel-manager
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bots.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_IDS
        sync: false
      - key: PORT
        value: 10000
